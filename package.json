{
  "name": "zcode-model-discovery",
  "version": "1.0.0",
  "description": "ZCode Desktop Extensions plugin to discover and autofill models from OpenAI-compatible /v1/models endpoints",
  "type": "module",
  "scripts": {
    "build": "bun build.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@notmike101/zcode-extension-sdk": "^0.3.9",
    "@types/bun": "^1.3.4",
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  }
}
